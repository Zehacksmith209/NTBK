# Dependencies — reinstalled with `npm install` and `pip install`, never committed
venv/
frontend/node_modules/

# Build output — regenerated by `npm run build`
frontend/dist/

# Python bytecode
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Editor / tooling state that is personal, not project
.claude/

# Notebook working folders: a Thermo.ntbk writes Thermo.files/ beside itself
*.files/

# Test fixture I generated while building the PDF feature
frontend/public/test-5page.pdf

# The original PyQt6 prototype. Still on disk, deliberately not in the repo —
# it lives at the v0.1.0-pyqt tag and the pyqt-prototype branch instead.
ntbk_app_old_python_prototype.py
Main_window.py
Canvas_base.py
Latex_box.py
